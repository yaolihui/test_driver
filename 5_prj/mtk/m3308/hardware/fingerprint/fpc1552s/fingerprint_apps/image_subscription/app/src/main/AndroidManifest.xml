<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fingerprints.imagesubscription"
    android:versionCode="0301"
    android:versionName="3.01">

    <uses-sdk
        android:minSdkVersion="23"
        android:targetSdkVersion="27" />

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.fingerprints.extension.ENGINEERING" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/fpc_icon"
        android:label="@string/app_name"
        android:theme="@style/FpcTheme">

        <uses-library android:name="com.fingerprints.extension" />

        <activity
            android:name=".activites.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".receiver.Receiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.ImageSubscriptionService"
            android:exported="true"
            android:icon="@drawable/fpc_icon"
            android:label="@string/app_label" />
    </application>
</manifest>
